import svelte from "@anarkisti/eslint-config/svelte";

import svelteConfig from "./svelte.config.js";

// Shared house preset (node base + eslint-plugin-svelte + TS parser wiring).
// It's a factory because it needs your svelte.config.js. See coding-style:svelte
// / the eslint-config repo. Add ignores for any vendored/generated dirs.
export default [
  ...svelte(svelteConfig),
  { ignores: ["dist/", "build/", ".svelte-kit/", "src/lib/vendor/", "static/vendor/"] },
];
